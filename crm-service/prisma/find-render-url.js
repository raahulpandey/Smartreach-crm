import * as fs from 'fs';
import * as path from 'path';
import { Database } from 'sqlite3'; // Wait, is sqlite3 installed? If not, we can use a pure JS parser or run a python script!
// Python has sqlite3 built-in! So a Python script is much more robust!
