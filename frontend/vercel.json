{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "VITE_API_URL": {
      "description": "The full base URL of the deployed CRM API (e.g. https://smartreach-crm-api.onrender.com/api)"
    }
  }
}
